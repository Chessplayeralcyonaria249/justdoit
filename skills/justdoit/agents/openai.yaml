interface:
  display_name: "justdoit"
  short_description: "Plan work, talk product-first, then ask before execution"
  default_prompt: "Use $justdoit to prepare plans.md, status.md, and test-plan.md for this task, then reply briefly in the same language with a product-level execution proposal and ask for confirmation before starting."
